spring:
  application:
    name: index-codes-service
  data:
    redis:
      host: localhost
      port: 6379

  zipkin:
    base-url: http://localhost:9411

server:
  port: 8011

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    enabled: false
