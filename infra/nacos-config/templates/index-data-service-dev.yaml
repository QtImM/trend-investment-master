spring:
  application:
    name: index-data-service
  data:
    redis:
      host: localhost
      port: 6379

  zipkin:
    base-url: http://localhost:9411

server:
  port: 8021

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    enabled: false
