spring:
  application:
    name: trend-trading-backtest-service

  zipkin:
    base-url: http://localhost:9411

server:
  port: 8051

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    enabled: false
