spring:
  application:
    name: trend-trading-backtest-service

  zipkin:
    base-url: http://localhost:9411

server:
  port: 8051

management:
  endpoints:
    web:
      exposure:
        include: "*"

nacos:
  discovery:
    server-addr: 127.0.0.1:8848
  config:
    server-addr: 127.0.0.1:8848
    file-extension: yaml
