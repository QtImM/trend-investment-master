spring:
  profiles: nacos
  application:
    name: trend-trading-backtest-view
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    bus:
      enabled: false
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        name: trend-trading-backtest-view-dev
