spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    bus:
      enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
